'use client';

import { DetectSection } from './section/index';

const DetectFeature: React.FC = () => {
  return (
    <main>
      <DetectSection />
    </main>
  );
};

export default DetectFeature;
