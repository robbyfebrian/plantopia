'use client';

import { DetectSection } from './sections/index';

const DetectFeature: React.FC = () => {
  return (
    <main>
      <DetectSection />
    </main>
  );
};

export default DetectFeature;
